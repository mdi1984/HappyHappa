<Page
    x:Class="HappyHappa.Pi.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HappyHappa.Pi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
      <TextBlock Text="Heyho" />
      <TextBlock>
        <Run Text="Command Rec. State: "/>
        <Run Text="{Binding CmdRecognizerState}" />
      </TextBlock>

      <TextBlock>
        <Run Text="Item Rec. State: "/>
        <Run Text="{Binding ItemRecognizerState}" />
      </TextBlock>

      <TextBlock>
        <Run Text="Item understood: "/>
        <Run Text="{Binding ItemUnderstood}" />
      </TextBlock>

      <TextBlock>
        <Run Text="Expirationdate understood: "/>
        <Run Text="{Binding DateUnderstood}" />
      </TextBlock>
      <TextBlock>
        <Run Text="Debug Message: "/>
        <Run Text="{Binding DebugMessage}" />
      </TextBlock>
    </StackPanel>
  </Grid>
</Page>
